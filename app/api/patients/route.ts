// TODO Phase 2 — Patients API
export async function GET() {
  return Response.json({ message: 'Patients API — Phase 2' });
}

export async function POST() {
  return Response.json({ message: 'Patients API — Phase 2' });
}
